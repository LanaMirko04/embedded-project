info:
  name: clearLocation
  type: http
  seq: 2

http:
  method: POST
  url: 127.0.0.1:5000/api/user/weather/clearLocation
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
