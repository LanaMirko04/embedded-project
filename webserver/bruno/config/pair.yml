info:
  name: pair
  type: http
  seq: 1

http:
  method: POST
  url: 127.0.0.1:5000/api/config/pair
  body:
    type: json
    data: |-
      {
        "token" : "navy-senior-5934"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
