info:
  name: clearLocation
  type: http
  seq: 8

http:
  method: POST
  url: 127.0.0.1:5000/api/config/clearLocation
  body:
    type: json
    data: |-
      {
        "token" : "navy-senior-5934"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
